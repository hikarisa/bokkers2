<h2>Editing Book</h2>
<%= form_with model: @book do |f| %>
  <label for="inputName">Title</label>
  <%= f.text_field :title, autofocus: true %>
  <label for="inputImage">Opinion</label>
  <%= f.text_area :body %>
  <%= f.submit "変更を保存" %>
  <div>
  <%= link_to "show", book_path(current_user) %>
  |
  <%= link_to "back", books_path(@books) %>
  </div>
<% end %>